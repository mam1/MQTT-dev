#!/bin/bash
#####################################
# 
#	 cp_deamon  status
#
#####################################

echo	"checking for active processes"
plist=$(pidof /usr/local/sbin/cp_daemon)
if [ "$plist" != "" ] 
	then
		echo "cp_deamon running\n"
		echo "$plist\n"
fi

exit 0